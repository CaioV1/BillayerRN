import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  viewContainer: { 
    flex: 1,
    paddingTop: 30
  },
  screenTopText: {
    fontSize: 25,
    marginHorizontal: 20,
  }
});